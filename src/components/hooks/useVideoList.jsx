import React, { useEffect } from "react";

const useVideoList = () => {
  useEffect(() => {
    async function fetchVideos() {}
  }, []);

  return (
    <div>
      <h1></h1>
    </div>
  );
};

export default useVideoList;
